import 'package:flutter/material.dart';

class ImagePostListPage extends StatelessWidget {
  const ImagePostListPage({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
